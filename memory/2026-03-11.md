# 2026-03-11

- Created Springfield Price Bot workspace.
- Added a price extraction pipeline for URLs, pasted HTML, and local HTML files.
- Added a standalone Telegram polling runner because no local OpenClaw CLI/runtime was available on this machine.
- Stored the Telegram bot token in the local workspace `.env` and excluded it from git.
- Deployed to Pi host AIBot (192.168.50.137) at /home/felixlee/Desktop/SpringfieldPriceBot.
- Registered OpenClaw agent id `springfield-price` and Telegram account id `springfieldprice` in /home/felixlee/.openclaw/openclaw.json on the Pi.
- Verified the Telegram bot token resolves to @Springfield_Price_Bot and confirmed the Pi gateway is listening on port 18789.
- Reworked the bot mission around UK food price intelligence, with source-aware routing for ONS, Food Foundation, community supermarket datasets, and Geolytix.
- Kept product-page URL and pasted-HTML extraction as a fallback for exact current page-price lookups.
- Replaced the original tests so they cover retailer comparison, official-source filtering, location guidance, and retained extraction behavior.
- Added a default nearby-store postcode of PA2 0SG, while still respecting explicit place names or postcodes in user location queries.
- Fixed the OpenClaw AGENTS command to resolve the pipeline from the current workspace or known Mac/Pi deployment paths, after the Pi agent failed on the Mac-only absolute path.
- Reset the stale Springfield Telegram OpenClaw session on the Pi after repeated old-path tool failures caused the model to stop invoking the pipeline; next Telegram message will create a fresh session.
- Added a direct community-dataset item lookup path in the pipeline, but the Pi still falls back to source guidance because Python 3.13 armv7 lacks an easy parquet runtime (`pyarrow` and `fastparquet` both hit unsupported build paths there).
- Replaced SpringfieldPriceBot direct item lookup with an offline CSV snapshot flow for Pi compatibility. New builder: `scripts/build_supermarket_latest_csv.py`. Snapshot file: `data/community_supermarket_latest.csv` built locally from the Kaggle-derived parquet mirror (130,633 latest rows). Pipeline now scans CSV via `csv.DictReader`, filters to food categories only, and no longer needs `fastparquet` or `pyarrow` on the Pi.
- Fixed live Telegram runtime selection: the Pi `.venv` lacked `requests`, so AGENTS now checks `import requests` before choosing the venv and falls back to `/usr/bin/python3` when needed. Cleared the stale Telegram main-session mapping and restarted the user OpenClaw gateway. Verified the exact AGENTS command returns the CSV-backed Wagyu result JSON on the Pi.
- Direct item lookup now prefers live retailer search pages for specialist product queries (currently Tom Hixson and Fine Food Specialist) before falling back to the local CSV snapshot. Added Shopify search-result parsers, source-aware replies, and an exclusion filter for obvious side-products like dripping, gin, pies, and gift boxes. Verified on the Pi and through the exact AGENTS runtime path after restarting the user OpenClaw gateway.
- Changed direct item lookup to shortlist retailers from the Kaggle-derived CSV first, then optionally query Bright Data Google Shopping for fresher live offers per shortlisted retailer before falling back to reachable retailer search pages and finally the CSV snapshot. Added Bright Data env/config docs, updated AGENTS to load workspace `.env`, added unit coverage for the CSV-shortlist-to-Bright-Data flow, redeployed to the Pi, reran Pi tests, and confirmed the gateway restarted cleanly. Bright Data credentials are not yet configured on the Pi, so the live bot currently stays on the existing retailer-search-page fallback path.
- Added optional PricesAPI support ahead of Bright Data in the live lookup order. The pipeline now does CSV shortlist -> PricesAPI -> Bright Data -> retailer search pages -> CSV fallback, with new unit coverage and README/.env.example updates. Synced the new code to the Pi, reran Pi tests (`14/14`), restarted `systemctl --user openclaw-gateway.service`, and confirmed both `SPRINGFIELD_PRICE_PRICESAPI_KEY` and Bright Data credentials are still missing on the Pi, so the live bot remains on retailer-search-page fallback for now.
- Added a live PricesAPI key to the Pi workspace `.env` and restarted the user OpenClaw gateway. Verified that PricesAPI authentication works and the configured `/products/search` plus `/products/{id}/offers` endpoints respond, but the returned catalog is currently too general for SpringfieldPriceBot's UK grocery shortlist (for example, `wagyu beef` resolves to non-supermarket snack products, and `eggs tesco` resolves to catalog items with zero UK offers). The deployed bot therefore still falls back correctly to retailer search pages for those grocery queries.
- Reordered SpringfieldPriceBot direct item lookup to: CSV shortlist -> retailer search pages -> PricesAPI -> Bright Data -> CSV fallback. Updated tests (`15/15` local and Pi), synced pipeline/tests/README to the Pi, verified a Pi smoke query still returns retailer search-page Wagyu offers by design under the new order, and restarted `systemctl --user openclaw-gateway.service` successfully.
